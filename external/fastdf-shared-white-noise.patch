diff --git a/include/fft.h b/include/fft.h
index fc7ecaf..70056c8 100644
--- a/include/fft.h
+++ b/include/fft.h
@@ -84,12 +84,14 @@ void fft_wavevector(int x, int y, int z, int N, double delta_k, double *kx,
                     double *ky, double *kz, double *k);
 void fft_execute(fftw_plan plan);
 
-/* Functions for ordinary contiguous arrays */
-int fft_normalize_r2c(fftw_complex *arr, int N, double boxlen);
-int fft_normalize_c2r(double *arr, int N, double boxlen);
+/* Functions for ordinary contiguous arrays.
+ * x_start/x_end let each node rank fill a contiguous x-slice of the (shared)
+ * array: a rank processes x in [x_start, x_end). */
+int fft_normalize_r2c(fftw_complex *arr, int N, double boxlen, int x_start, int x_end);
+int fft_normalize_c2r(double *arr, int N, double boxlen, int x_start, int x_end);
 int fft_apply_kernel(fftw_complex *write, const fftw_complex *read, int N,
                      double boxlen, void (*compute)(struct kernel* the_kernel),
-                     const void *params);
+                     const void *params, int x_start, int x_end);
 
 
 #endif
diff --git a/include/input.h b/include/input.h
index eaaae7c..acfca66 100644
--- a/include/input.h
+++ b/include/input.h
@@ -141,6 +141,11 @@ int readFieldFileDataSet(double **box, int *N, double *box_len,
 
 int readFieldFile_MPI(double **box, int *N, double *box_len, MPI_Comm comm,
                       const char *fname);
+/* Read only the grid size and box length (called by node_rank 0; the caller
+ * broadcasts the result). */
+int readFieldHeader(int *N, double *box_len, const char *fname, const char *dset_name);
+/* Read the full N^3 field into a pre-allocated buffer (called by node_rank 0). */
+int readFieldBody(double *box, const char *fname, const char *dset_name, int N);
 int fileExists(const char *fname);
 int groupExists(const char *fname, const char *group_name);
 
diff --git a/src/fft.c b/src/fft.c
index 47db388..23fe58b 100644
--- a/src/fft.c
+++ b/src/fft.c
@@ -35,9 +35,9 @@ void fft_wavevector(int x, int y, int z, int N, double delta_k, double *kx,
 }
 
 /* Normalize the complex array after transforming to momentum space */
-int fft_normalize_r2c(fftw_complex *arr, int N, double boxlen) {
+int fft_normalize_r2c(fftw_complex *arr, int N, double boxlen, int x_start, int x_end) {
     const double boxvol = boxlen*boxlen*boxlen;
-    for (int x=0; x<N; x++) {
+    for (int x=x_start; x<x_end; x++) {
         for (int y=0; y<N; y++) {
             for (int z=0; z<=N/2; z++) {
                 arr[row_major_half(x, y, z, N)] *= boxvol/(N*N*N);
@@ -49,9 +49,9 @@ int fft_normalize_r2c(fftw_complex *arr, int N, double boxlen) {
 }
 
 /* Normalize the real array after transforming to configuration space */
-int fft_normalize_c2r(double *arr, int N, double boxlen) {
+int fft_normalize_c2r(double *arr, int N, double boxlen, int x_start, int x_end) {
     const double boxvol = boxlen*boxlen*boxlen;
-    for (int x=0; x<N; x++) {
+    for (int x=x_start; x<x_end; x++) {
         for (int y=0; y<N; y++) {
             for (int z=0; z<N; z++) {
                 arr[row_major(x, y, z, N)] /= boxvol;
@@ -70,11 +70,11 @@ void fft_execute(fftw_plan plan) {
 /* Apply a kernel to a 3D array after transforming to momentum space */
 int fft_apply_kernel(fftw_complex *write, const fftw_complex *read, int N,
                      double boxlen, void (*compute)(struct kernel* the_kernel),
-                     const void *params) {
+                     const void *params, int x_start, int x_end) {
     const double dk = 2 * M_PI / boxlen;
 
     #pragma omp parallel for
-    for (int x=0; x<N; x++) {
+    for (int x=x_start; x<x_end; x++) {
         for (int y=0; y<N; y++) {
             for (int z=0; z<=N/2; z++) {
                 /* Calculate the wavevector */
diff --git a/src/input.c b/src/input.c
index 7bdd627..458f483 100644
--- a/src/input.c
+++ b/src/input.c
@@ -463,6 +463,92 @@ int readFieldFile_MPI(double **box, int *N, double *box_len, MPI_Comm comm,
 #endif
 }
 
+/* Read only the grid size N and box length from the white-noise file.
+ * This is called by node_rank 0; the caller broadcasts the result to the
+ * other ranks in the node. */
+int readFieldHeader(int *N, double *box_len, const char *fname, const char *dset_name) {
+    /* Open the hdf5 file */
+    hid_t h_file = H5Fopen(fname, H5F_ACC_RDONLY, H5P_DEFAULT);
+
+    /* Check if the Header group exists */
+    hid_t h_status = H5Eset_auto1(NULL, NULL); // turn off error printing
+    h_status = H5Gget_objinfo(h_file, "/Header", 0, NULL);
+
+    /* If the group exists, read the box size attribute */
+    if (h_status == 0) {
+        hid_t h_grp = H5Gopen(h_file, "Header", H5P_DEFAULT);
+        hid_t h_attr = H5Aopen(h_grp, "BoxSize", H5P_DEFAULT);
+        double boxsize[3];
+        if (H5Aread(h_attr, H5T_NATIVE_DOUBLE, &boxsize) < 0) {
+            printf("Error reading hdf5 attribute '%s'.\n", "BoxSize");
+            return 1;
+        }
+        assert(boxsize[0] == boxsize[1]);
+        assert(boxsize[1] == boxsize[2]);
+        *box_len = boxsize[0];
+        H5Aclose(h_attr);
+        H5Gclose(h_grp);
+    }
+
+    /* Open the Field dataset and fetch the grid dimensions */
+    hid_t h_data = H5Dopen2(h_file, dset_name, H5P_DEFAULT);
+    hid_t h_space = H5Dget_space(h_data);
+    int ndims = H5Sget_simple_extent_ndims(h_space);
+    hsize_t *dims = malloc(ndims * sizeof(hsize_t));
+    H5Sget_simple_extent_dims(h_space, dims, NULL);
+    int read_N = dims[0];
+
+    /* We should be in 3D */
+    if (ndims != 3) {
+        printf("Number of dimensions %d != 3.\n", ndims);
+        return 2;
+    }
+    /* It should be a cube (but allow for padding in the last dimension) */
+    if (read_N != dims[1] || (read_N != dims[2] && (read_N + 2) != dims[2])) {
+        printf("Non-cubic grid size (%lld, %lld, %lld).\n", dims[0], dims[1], dims[2]);
+        return 2;
+    }
+    *N = read_N;
+
+    H5Sclose(h_space);
+    H5Dclose(h_data);
+    H5Fclose(h_file);
+    free(dims);
+
+    return 0;
+}
+
+/* Read the full N^3 white-noise field into a pre-allocated buffer.
+ * Called by node_rank 0; the field is then shared via a memory window. */
+int readFieldBody(double *box, const char *fname, const char *dset_name, int N) {
+    /* Open the hdf5 file */
+    hid_t h_file = H5Fopen(fname, H5F_ACC_RDONLY, H5P_DEFAULT);
+
+    /* Open the Field dataset */
+    hid_t h_data = H5Dopen2(h_file, dset_name, H5P_DEFAULT);
+    hid_t h_space = H5Dget_space(h_data);
+
+    /* The hyperslab that should be read (needed in case of padding) */
+    const hsize_t space_rank = 3;
+    const hsize_t space_dims[3] = {N, N, N};
+    const hsize_t space_offset[3] = {0, 0, 0};
+
+    hid_t h_memspace = H5Screate_simple(space_rank, space_dims, NULL);
+    H5Sselect_hyperslab(h_space, H5S_SELECT_SET, space_offset, NULL, space_dims, NULL);
+
+    hid_t h_err = H5Dread(h_data, H5T_NATIVE_DOUBLE, h_memspace, h_space, H5P_DEFAULT, box);
+    if (h_err < 0) {
+        printf("Error reading hdf5 file '%s'.\n", fname);
+        return 1;
+    }
+
+    H5Sclose(h_memspace);
+    H5Sclose(h_space);
+    H5Dclose(h_data);
+    H5Fclose(h_file);
+
+    return 0;
+}
 
 int fileExists(const char *fname) {
     int exists = 0;
diff --git a/src/runner.c b/src/runner.c
index c0c8a98..b958e20 100644
--- a/src/runner.c
+++ b/src/runner.c
@@ -38,6 +38,21 @@ long long run_fastdf(struct params *pars, struct units *us) {
     MPI_Comm_rank(MPI_COMM_WORLD, &rank);
     MPI_Comm_size(MPI_COMM_WORLD, &MPI_Rank_Count);
 
+    /* Node-local communicator (ranks sharing physical memory) */
+    MPI_Comm node_comm;
+    MPI_Comm_split_type(MPI_COMM_WORLD, MPI_COMM_TYPE_SHARED, 0, MPI_INFO_NULL, &node_comm);
+    int node_rank, node_size;
+    MPI_Comm_rank(node_comm, &node_rank);
+    MPI_Comm_size(node_comm, &node_size);
+
+    /* Let the FFT (executed by node_rank 0) use all threads of the node */
+    int omp_threads = 1;
+    const char *omp_env = getenv("OMP_NUM_THREADS");
+    if (omp_env) omp_threads = atoi(omp_env);
+    if (omp_threads < 1) omp_threads = 1;
+    fftw_init_threads();
+    fftw_plan_with_nthreads(node_size * omp_threads);
+
     /* The level of verbosity */
     const int verbosity = pars->Verbosity;
     const int verbosity_low = rank + (verbosity == 0); // low priority
@@ -185,7 +200,9 @@ long long run_fastdf(struct params *pars, struct units *us) {
         message(verbosity_low, "\n\n");
     }
 
-    /* Read the Gaussian random field on each MPI rank */
+    /* Read the Gaussian random field into a per-node shared-memory window.
+     * All grids are shared among the ranks of a node: node_rank 0 computes or
+     * reads each one once, and every rank reads them for the particle loop. */
     double *box;
     double BoxLen = 0;
     int N;
@@ -193,9 +210,71 @@ long long run_fastdf(struct params *pars, struct units *us) {
     header(verbosity_low, "Random phases");
     message(verbosity_low, "Reading Gaussian random field from %s (dataset %s).\n", pars->GaussianRandomFieldFile, pars->GaussianRandomFieldDataset);
 
-    readFieldFileDataSet(&box, &N, &BoxLen, pars->GaussianRandomFieldFile, pars->GaussianRandomFieldDataset);
-    // readFieldFile(&box, &N, &BoxLen, pars->GaussianRandomFieldFile);
-    // readFieldFile_MPI(&box, &N, &BoxLen, MPI_COMM_WORLD, pars->GaussianRandomFieldFile);
+    /* Read the grid size + box length on node_rank 0, broadcast within the node */
+    if (node_rank == 0)
+        readFieldHeader(&N, &BoxLen, pars->GaussianRandomFieldFile, pars->GaussianRandomFieldDataset);
+    MPI_Bcast(&N, 1, MPI_INT, 0, node_comm);
+    MPI_Bcast(&BoxLen, 1, MPI_DOUBLE, 0, node_comm);
+
+    /* One shared window sized to the *peak* simultaneous footprint rather than
+     * the sum of all grids. Fixed slots: box (1 real) + fbox, fgrf (2 complex).
+     * Scratch slots: 7 real + 2 complex, reused across the three phases because
+     * the phase-1/2/3 grids have disjoint lifetimes. */
+    size_t n3 = (size_t)N * N * N;
+    size_t real_sz = n3 * sizeof(double);
+    size_t cplx_sz = n3 * sizeof(fftw_complex);
+    size_t total_sz = 8 * real_sz + 4 * cplx_sz;
+
+    void *sh_base = NULL;
+    MPI_Win sh_win;
+    MPI_Aint local_sz = (node_rank == 0) ? (MPI_Aint)total_sz : 0;
+    MPI_Win_allocate_shared(local_sz, sizeof(double), MPI_INFO_NULL, node_comm, &sh_base, &sh_win);
+    MPI_Aint sh_size; int sh_disp;
+    MPI_Win_shared_query(sh_win, 0, &sh_size, &sh_disp, &sh_base);
+
+    /* Fixed grids (alive throughout) */
+    char *shb = (char *)sh_base;
+    box              = (double *)(shb + 0 * real_sz);
+    fftw_complex *fbox = (fftw_complex *)(shb + 1 * real_sz);
+    fftw_complex *fgrf = (fftw_complex *)(shb + 1 * real_sz + 1 * cplx_sz);
+
+    /* Scratch region (reused across phases) */
+    char *scr = shb + 1 * real_sz + 2 * cplx_sz;
+
+    /* Real grids: box_dic/box_phi0/box_dshift share slot 0;
+     * box_tic/box_phi1/box_tshift share slot 1. */
+    double *box_dic     = (double *)(scr + 0 * real_sz);
+    double *box_tic     = (double *)(scr + 1 * real_sz);
+    double *box_phi0    = (double *)(scr + 0 * real_sz);
+    double *box_phi1    = (double *)(scr + 1 * real_sz);
+    double *box_psi0    = (double *)(scr + 2 * real_sz);
+    double *box_psi1    = (double *)(scr + 3 * real_sz);
+    double *box_phi     = (double *)(scr + 4 * real_sz);
+    double *box_psi     = (double *)(scr + 5 * real_sz);
+    double *box_phi_dot = (double *)(scr + 6 * real_sz);
+    double *box_dshift  = (double *)(scr + 0 * real_sz);
+    double *box_tshift  = (double *)(scr + 1 * real_sz);
+
+    /* Complex grids: fbox_phi/fbox_psi use the tail of the scratch; the phase-3
+     * gauge temporaries overlap the phase-2 real slots (disjoint lifetimes). */
+    fftw_complex *fbox_phi     = (fftw_complex *)(scr + 7 * real_sz);
+    fftw_complex *fbox_psi     = (fftw_complex *)(scr + 7 * real_sz + 1 * cplx_sz);
+    fftw_complex *fbox_hdot    = (fftw_complex *)(scr + 2 * real_sz);
+    fftw_complex *fbox_etadot  = (fftw_complex *)(scr + 4 * real_sz);
+    fftw_complex *fbox_Nbshift = (fftw_complex *)(scr + 6 * real_sz);
+    fftw_complex *fbox_alpha   = (fftw_complex *)(scr + 8 * real_sz);
+    fftw_complex *fbox_HTNbp   = (fftw_complex *)(scr + 2 * real_sz);
+
+    /* node_rank 0 reads the white-noise field into the shared window */
+    if (node_rank == 0)
+        readFieldBody(box, pars->GaussianRandomFieldFile, pars->GaussianRandomFieldDataset, N);
+    MPI_Win_fence(0, sh_win);
+
+    /* x-range and flat-range each node rank fills (block decomposition) */
+    const int x0 = node_rank * N / node_size;
+    const int x1 = (node_rank + 1) * N / node_size;
+    const size_t i0 = (size_t)node_rank * n3 / node_size;
+    const size_t i1 = (size_t)(node_rank + 1) * n3 / node_size;
 
     /* Override the box length? */
     if (BoxLen > 0.) {
@@ -219,65 +298,74 @@ long long run_fastdf(struct params *pars, struct units *us) {
         exit(1);
     }
 
-    /* Do we want to invert the field for paired simulations? */
-    if (pars->InvertField) {
-        message(verbosity_low, "Inverting input field.\n");
-        for (int i = 0; i < N * N * N; i++) {
-            box[i] *= -1.0;
+    /* Invert / normalize the shared white-noise field. Only node_rank 0 writes
+     * it (it is shared, so a single writer avoids races). */
+    if (node_rank == 0) {
+        /* Do we want to invert the field for paired simulations? */
+        if (pars->InvertField) {
+            message(verbosity_low, "Inverting input field.\n");
+            for (int i = 0; i < N * N * N; i++) {
+                box[i] *= -1.0;
+            }
         }
-    }
 
-    /* Compute the mean of the input field */
-    double mean = 0.;
-    for (int i = 0; i < N * N * N; i++) {
-        mean += box[i];
-    }
-    mean /= (double) (N * N * N);
+        /* Compute the mean of the input field */
+        double mean = 0.;
+        for (int i = 0; i < N * N * N; i++) {
+            mean += box[i];
+        }
+        mean /= (double) (N * N * N);
 
-    /* Compute the variance of the input field */
-    double var = 0.;
-    for (int i = 0; i < N * N * N; i++) {
-        var += (box[i] - mean) * (box[i] - mean);
-    }
-    var /= (double) (N * N * N);
+        /* Compute the variance of the input field */
+        double var = 0.;
+        for (int i = 0; i < N * N * N; i++) {
+            var += (box[i] - mean) * (box[i] - mean);
+        }
+        var /= (double) (N * N * N);
 
-    message(verbosity_low, "Mean = %.10g\n", mean);
-    message(verbosity_low, "Variance = %.10g\n", var);
+        message(verbosity_low, "Mean = %.10g\n", mean);
+        message(verbosity_low, "Variance = %.10g\n", var);
 
-    /* Do we want to normalize the Gaussian field? */
-    if (pars->NormalizeGaussianField) {
-        message(verbosity_low, "\nNormalizing the input field.\n");
+        /* Do we want to normalize the Gaussian field? */
+        if (pars->NormalizeGaussianField) {
+            message(verbosity_low, "\nNormalizing the input field.\n");
 
-        /* Set the variance to 1.0, then apply the correct normalization */
-        const double inv_sdev = 1.0 / sqrt(var);
-        const double norm = inv_sdev * pow(N / BoxLen, 1.5);
-        message(verbosity_low, "Normalizing by %g.\n", norm);
+            /* Set the variance to 1.0, then apply the correct normalization */
+            const double inv_sdev = 1.0 / sqrt(var);
+            const double norm = inv_sdev * pow(N / BoxLen, 1.5);
+            message(verbosity_low, "Normalizing by %g.\n", norm);
 
-        for (int i = 0; i < N * N * N; i++) {
-            box[i] *= norm;
+            for (int i = 0; i < N * N * N; i++) {
+                box[i] *= norm;
+            }
         }
-    }
 
-    /* Do we want to correct the Monofonic normalization? */
-    if (pars->AssumeMonofonicNormalization) {
-        message(verbosity_low, "\nUndoing the monofonIC normalization.\n");
+        /* Do we want to correct the Monofonic normalization? */
+        if (pars->AssumeMonofonicNormalization) {
+            message(verbosity_low, "\nUndoing the monofonIC normalization.\n");
 
-        /* Apply the correct normalization, relative to monofonIC */
-        const double norm = pow(h / (2.0 * M_PI), 1.5);
-        message(verbosity_low, "Normalizing by %g.\n", norm);
+            /* Apply the correct normalization, relative to monofonIC */
+            const double norm = pow(h / (2.0 * M_PI), 1.5);
+            message(verbosity_low, "Normalizing by %g.\n", norm);
 
-        for (int i = 0; i < N * N * N; i++) {
-            box[i] *= norm;
+            for (int i = 0; i < N * N * N; i++) {
+                box[i] *= norm;
+            }
         }
     }
+    MPI_Win_fence(0, sh_win);
+
+    /* Fourier transform the Gaussian random field (node_rank 0 only; it uses
+     * all node threads via fftw_plan_with_nthreads). */
+    if (node_rank == 0) {
+        fftw_plan r2c = fftw_plan_dft_r2c_3d(N, N, N, box, fbox, FFTW_ESTIMATE);
+        fft_execute(r2c);
+        fftw_destroy_plan(r2c);
+    }
+    MPI_Win_fence(0, sh_win);
 
-    /* Fourier transform the Gaussian random field */
-    fftw_complex *fbox = malloc(N*N*N*sizeof(fftw_complex));
-    fftw_complex *fgrf = malloc(N*N*N*sizeof(fftw_complex));
-    fftw_plan r2c = fftw_plan_dft_r2c_3d(N, N, N, box, fbox, FFTW_ESTIMATE);
-    fft_execute(r2c);
-    fft_normalize_r2c(fbox, N, BoxLen);
-    fftw_destroy_plan(r2c);
+    /* Normalize the Fourier field (block-decomposed across node ranks) */
+    fft_normalize_r2c(fbox, N, BoxLen, x0, x1);
 
     /* Do we need to apply the primordial power spectrum? */
     if (pars->NormalizeGaussianField || pars->AssumeMonofonicNormalization) {
@@ -296,11 +384,12 @@ long long run_fastdf(struct params *pars, struct units *us) {
         message(verbosity_low, "beta_s = %.5g\n", ps.beta_s);
 
         /* Apply the bare power spectrum to fbox */
-        fft_apply_kernel(fbox, fbox, N, BoxLen, kernel_power_no_transfer, &ps);
+        fft_apply_kernel(fbox, fbox, N, BoxLen, kernel_power_no_transfer, &ps, x0, x1);
     }
 
-    /* Make a copy of the complex Gaussian random field */
-    memcpy(fgrf, fbox, N*N*N*sizeof(fftw_complex));
+    /* Make a copy of the complex Gaussian random field (block-decomposed) */
+    memcpy(fgrf + (size_t)x0 * N * (N/2 + 1), fbox + (size_t)x0 * N * (N/2 + 1),
+           (size_t)(x1 - x0) * N * (N/2 + 1) * sizeof(fftw_complex));
 
     /* Find the relevant density title among the transfer functions */
     char *title = pars->TransferFunctionDensity;
@@ -382,10 +471,7 @@ long long run_fastdf(struct params *pars, struct units *us) {
     double z_begin = 1./a_begin - 1;
     double log_tau_begin = perturbLogTauAtRedshift(&spline, z_begin);
 
-    /* Allocate boxes for the initial conditions */
-    double *box_dic = malloc(N*N*N*sizeof(double));
-    double *box_tic = malloc(N*N*N*sizeof(double));
-
+    /* (box_dic, box_tic are already carved from the shared window) */
 
     header(verbosity_low, "Generating pre-initial conditions");
     message(verbosity_low, "Generating pre-initial grids.\n");
@@ -407,24 +493,31 @@ long long run_fastdf(struct params *pars, struct units *us) {
         struct spline_params sp = {&spline, index_psi, tau_index, u_tau};
 
         /* Apply the transfer function (read only fgrf, output into fbox) */
-        fft_apply_kernel(fbox, fgrf, N, BoxLen, kernel_transfer_function, &sp);
+        fft_apply_kernel(fbox, fgrf, N, BoxLen, kernel_transfer_function, &sp, x0, x1);
 
-        /* Fourier transform to real space */
-        fftw_plan c2r = fftw_plan_dft_c2r_3d(N, N, N, fbox, box, FFTW_ESTIMATE);
-        fft_execute(c2r);
-        fft_normalize_c2r(box, N, BoxLen);
-        fftw_destroy_plan(c2r);
+        /* Fourier transform to real space (node_rank 0 only) */
+        MPI_Win_fence(0, sh_win);
+        if (node_rank == 0) {
+            fftw_plan c2r = fftw_plan_dft_c2r_3d(N, N, N, fbox, box, FFTW_ESTIMATE);
+            fft_execute(c2r);
+            fftw_destroy_plan(c2r);
+        }
+        MPI_Win_fence(0, sh_win);
+
+        fft_normalize_c2r(box, N, BoxLen, x0, x1);
 
         /* Copy into the boxes for the density and velocity perturbations */
-        memcpy(box_dic, box, N*N*N*sizeof(double));
-        memcpy(box_tic, box, N*N*N*sizeof(double));
+        memcpy(box_dic + i0, box + i0, (i1 - i0) * sizeof(double));
+        memcpy(box_tic + i0, box + i0, (i1 - i0) * sizeof(double));
 
         /* Multiply by the appropriate factor */
-        for (int i=0; i<N*N*N; i++) {
+        for (size_t i = i0; i < i1; i++) {
             box_dic[i] *= -2 * inv_c2;
             box_tic[i] *= 0.5 * exp(log_tau_begin);
         }
 
+        MPI_Win_fence(0, sh_win);
+
         if (rank == 0 && pars->OutputFields) {
             char dnu_fname[50];
             sprintf(dnu_fname, "%s/ic_dnu.hdf5", pars->OutputDirectory);
@@ -490,9 +583,7 @@ long long run_fastdf(struct params *pars, struct units *us) {
         p->v_i_mag = hypot3(p->v[0], p->v[1], p->v[2]);
     }
 
-    /* Free the boxes used for the initial conditions */
-    free(box_dic);
-    free(box_tic);
+    /* (box_dic, box_tic are shared and released with the window at the end) */
 
     message(verbosity_low, "Done with pre-initial conditions.\n");
 
@@ -524,23 +615,8 @@ long long run_fastdf(struct params *pars, struct units *us) {
 
     message(verbosity_high, "     ITER a         z         I            recompute\n");
 
-    /* Allocate grids for the potentials (phi and psi) at two different times
-     * t0 <= t < t1. We will evaluate at time t using linear interpolation. */
-    double *box_phi0 = malloc(N*N*N*sizeof(double));
-    double *box_phi1 = malloc(N*N*N*sizeof(double));
-    double *box_psi0 = malloc(N*N*N*sizeof(double));
-    double *box_psi1 = malloc(N*N*N*sizeof(double));
-
-    /* The interpolated grids at time t */
-    double *box_phi = malloc(N*N*N*sizeof(double));
-    double *box_psi = malloc(N*N*N*sizeof(double));
-
-    /* Time derivative at time t */
-    double *box_phi_dot = malloc(N*N*N*sizeof(double));
-
-    /* Complex grids that will be used */
-    fftw_complex *fbox_phi = malloc(N*N*N*sizeof(fftw_complex));
-    fftw_complex *fbox_psi = malloc(N*N*N*sizeof(fftw_complex));
+    /* (box_phi0/1, box_psi0/1, box_phi, box_psi, box_phi_dot, fbox_phi, fbox_psi
+     * are already carved from the shared window) */
 
     /* Transer functions evaluated at a reference scale during the last update */
     double Tk_phi = 0.0;
@@ -662,7 +738,7 @@ long long run_fastdf(struct params *pars, struct units *us) {
             Tk_psi = Tk_psi_curr;
 
             /* Copy the current boxes at t=t1 to the slots for t=t0 */
-            if (ITER > 0) {
+            if (node_rank == 0 && ITER > 0) {
                 memcpy(box_phi0, box_phi1, N*N*N*sizeof(double));
                 memcpy(box_psi0, box_psi1, N*N*N*sizeof(double));
             }
@@ -674,19 +750,23 @@ long long run_fastdf(struct params *pars, struct units *us) {
             struct spline_params sp_psi_curr = {&spline, index_psi, tau_idx_major_next, u_major_next};
 
             /* Apply the transfer function (read only fgrf, output into fbox) */
-            fft_apply_kernel(fbox_phi, fgrf, N, BoxLen, kernel_transfer_function, &sp_phi_curr);
-            fft_apply_kernel(fbox_psi, fgrf, N, BoxLen, kernel_transfer_function, &sp_psi_curr);
-
-            /* Fourier transform to real space */
-            fftw_plan c2r = fftw_plan_dft_c2r_3d(N, N, N, fbox_phi, box_phi1, FFTW_ESTIMATE);
-            fft_execute(c2r);
-            fft_normalize_c2r(box_phi1, N, BoxLen);
-            fftw_destroy_plan(c2r);
-
-            fftw_plan c2r2 = fftw_plan_dft_c2r_3d(N, N, N, fbox_psi, box_psi1, FFTW_ESTIMATE);
-            fft_execute(c2r2);
-            fft_normalize_c2r(box_psi1, N, BoxLen);
-            fftw_destroy_plan(c2r2);
+            fft_apply_kernel(fbox_phi, fgrf, N, BoxLen, kernel_transfer_function, &sp_phi_curr, x0, x1);
+            fft_apply_kernel(fbox_psi, fgrf, N, BoxLen, kernel_transfer_function, &sp_psi_curr, x0, x1);
+
+            /* Fourier transform to real space (node_rank 0 only) */
+            MPI_Win_fence(0, sh_win);
+            if (node_rank == 0) {
+                fftw_plan c2r = fftw_plan_dft_c2r_3d(N, N, N, fbox_phi, box_phi1, FFTW_ESTIMATE);
+                fft_execute(c2r);
+                fft_normalize_c2r(box_phi1, N, BoxLen, 0, N);
+                fftw_destroy_plan(c2r);
+
+                fftw_plan c2r2 = fftw_plan_dft_c2r_3d(N, N, N, fbox_psi, box_psi1, FFTW_ESTIMATE);
+                fft_execute(c2r2);
+                fft_normalize_c2r(box_psi1, N, BoxLen, 0, N);
+                fftw_destroy_plan(c2r2);
+            }
+            MPI_Win_fence(0, sh_win);
         }
 
         /* Skip the particle integration during the first step */
@@ -695,17 +775,19 @@ long long run_fastdf(struct params *pars, struct units *us) {
 
         /* Linearly interpolate the potentials to the current time step */
         double o = (log_tau - log_tau_major_prev) / (log_tau_major_next - log_tau_major_prev);
-        for (int i=0; i<N*N*N; i++) {
+        for (size_t i = i0; i < i1; i++) {
             box_phi[i] = o * box_phi1[i] + (1.0 - o) * box_phi0[i];
             box_psi[i] = o * box_psi1[i] + (1.0 - o) * box_psi0[i];
         }
 
         /* Compute the potential derivative grid */
         double inv_delta_tau = 1.0 / (exp(log_tau_major_next) - exp(log_tau_major_prev));
-        for (int i=0; i<N*N*N; i++) {
+        for (size_t i = i0; i < i1; i++) {
             box_phi_dot[i] = (box_phi1[i] - box_phi0[i]) * inv_delta_tau;
         }
 
+        MPI_Win_fence(0, sh_win);
+
         if (rank == 0 && ((pars->OutputFields > 1 && recompute) || (pars->OutputFields > 2 && ITER % 10 == 0))) {
             char psi_fname[50];
             sprintf(psi_fname, "%s/psi_%d.hdf5", pars->OutputDirectory, ITER);
@@ -808,13 +890,15 @@ long long run_fastdf(struct params *pars, struct units *us) {
             p->x[1] += p->v[1] * drift * dtau;
             p->x[2] += p->v[2] * drift * dtau;
         }
+        MPI_Win_fence(0, sh_win);
 
         /* Linearly interpolate the potentials to the half-time step */
         double o2 = (log_tau_half - log_tau_major_prev) / (log_tau_major_next - log_tau_major_prev);
-        for (int i=0; i<N*N*N; i++) {
+        for (size_t i = i0; i < i1; i++) {
             box_phi[i] = o2 * box_phi1[i] + (1.0 - o2) * box_phi0[i];
             box_psi[i] = o2 * box_psi1[i] + (1.0 - o2) * box_psi0[i];
         }
+        MPI_Win_fence(0, sh_win);
 
         /* Integrate the particles during the second half-step */
         #pragma omp parallel for
@@ -883,6 +967,7 @@ long long run_fastdf(struct params *pars, struct units *us) {
             p->v[1] += q_vec[1] * phi_dot_c2 * dtau2;
             p->v[2] += q_vec[2] * phi_dot_c2 * dtau2;
         }
+        MPI_Win_fence(0, sh_win);
 
         /* Step forward */
         a = a_next;
@@ -913,22 +998,11 @@ long long run_fastdf(struct params *pars, struct units *us) {
         }
     }
 
-    /* Free grids that are no longer needed */
-    free(fbox_phi);
-    free(fbox_psi);
-    free(box_phi);
-    free(box_psi);
-    free(box_phi0);
-    free(box_phi1);
-    free(box_psi0);
-    free(box_psi1);
-    free(box_phi_dot);
+    /* (phase-2 grids are shared and released with the window at the end) */
 
     if (gauge_Nbody) {
 
-        /* Allocate boxes for the (density and flux) gauge transformations */
-        double *box_dshift = malloc(N*N*N*sizeof(double));
-        double *box_tshift = malloc(N*N*N*sizeof(double));
+        /* (box_dshift, box_tshift are already carved from the shared window) */
 
         header(verbosity_low, "Generating N-body gauge transformation grid");
 
@@ -974,28 +1048,20 @@ long long run_fastdf(struct params *pars, struct units *us) {
             struct spline_params sp_Nbshift = {&spline, index_Nbshift, tau_index, u_tau};
             struct spline_params sp_HTNbp = {&spline, index_HTNbp, tau_index, u_tau};
 
-            /* Allocate boxes for the complex terms needed for the gauge transformation */
-            fftw_complex *fbox_alpha = malloc(N*N*N*sizeof(fftw_complex));
-            fftw_complex *fbox_hdot = malloc(N*N*N*sizeof(fftw_complex));
-            fftw_complex *fbox_etadot = malloc(N*N*N*sizeof(fftw_complex));
-            fftw_complex *fbox_Nbshift = malloc(N*N*N*sizeof(fftw_complex));
+            /* (fbox_alpha/hdot/etadot/Nbshift are carved from the shared window) */
 
             /* Apply the transfer function (read only fgrf, output into fbox) */
-            fft_apply_kernel(fbox_hdot, fgrf, N, BoxLen, kernel_transfer_function, &sp_hdot);
-            fft_apply_kernel(fbox_etadot, fgrf, N, BoxLen, kernel_transfer_function, &sp_etadot);
-            fft_apply_kernel(fbox_Nbshift, fgrf, N, BoxLen, kernel_transfer_function, &sp_Nbshift);
+            fft_apply_kernel(fbox_hdot, fgrf, N, BoxLen, kernel_transfer_function, &sp_hdot, x0, x1);
+            fft_apply_kernel(fbox_etadot, fgrf, N, BoxLen, kernel_transfer_function, &sp_etadot, x0, x1);
+            fft_apply_kernel(fbox_Nbshift, fgrf, N, BoxLen, kernel_transfer_function, &sp_Nbshift, x0, x1);
 
             /* Compute alpha * k^2 = h_dot + 6 * eta_dot */
-            for (int i=0; i<N*N*(N/2+1); i++) {
+            for (int i = x0 * N * (N/2 + 1); i < x1 * N * (N/2 + 1); i++) {
                 fbox_alpha[i] = fbox_hdot[i] + 6.0 * fbox_etadot[i];
             }
 
             /* Apply the inverse Poisson kernel -1/k^2 */
-            fft_apply_kernel(fbox_alpha, fbox_alpha, N, BoxLen, kernel_inv_poisson, NULL);
-
-            /* Free grids that are no longer needed */
-            free(fbox_hdot);
-            free(fbox_etadot);
+            fft_apply_kernel(fbox_alpha, fbox_alpha, N, BoxLen, kernel_inv_poisson, NULL, x0, x1);
 
             /* Compute the conformal time derivative of the background density */
             double Omega_nu = perturbDensityAtLogTau(&spline, log_tau_end, index_ncdm);
@@ -1020,19 +1086,19 @@ long long run_fastdf(struct params *pars, struct units *us) {
             double rho_dot_rho_c4 = rho_dot_rho / (c * c * c * c);
 
             /* Compute the total density gauge shift */
-            for (int i=0; i<N*N*N; i++) {
+            for (int i = x0 * N * (N/2 + 1); i < x1 * N * (N/2 + 1); i++) {
                 fbox[i] = -0.5 * fbox_alpha[i] * rho_dot_rho_c4 - (1.0 + w_ncdm) * fbox_Nbshift[i];
             }
 
-            /* Free grids that are no longer needed */
-            free(fbox_alpha);
-            free(fbox_Nbshift);
-
-            /* Fourier transform to real space */
-            fftw_plan c2r = fftw_plan_dft_c2r_3d(N, N, N, fbox, box_dshift, FFTW_ESTIMATE);
-            fft_execute(c2r);
-            fft_normalize_c2r(box_dshift, N, BoxLen);
-            fftw_destroy_plan(c2r);
+            /* Fourier transform to real space (node_rank 0 only) */
+            MPI_Win_fence(0, sh_win);
+            if (node_rank == 0) {
+                fftw_plan c2r = fftw_plan_dft_c2r_3d(N, N, N, fbox, box_dshift, FFTW_ESTIMATE);
+                fft_execute(c2r);
+                fft_normalize_c2r(box_dshift, N, BoxLen, 0, N);
+                fftw_destroy_plan(c2r);
+            }
+            MPI_Win_fence(0, sh_win);
 
             if (rank == 0 && pars->OutputFields) {
                 char density_fname[50];
@@ -1040,29 +1106,29 @@ long long run_fastdf(struct params *pars, struct units *us) {
                 writeFieldFile(box_dshift, N, BoxLen, density_fname);
             }
 
-            /* Allocate new complex grid */
-            fftw_complex *fbox_HTNbp = malloc(N*N*N*sizeof(fftw_complex));
+            /* (fbox_HTNbp is carved from the shared window) */
 
             /* Apply the transfer function for H_T_Nb_prime */
-            fft_apply_kernel(fbox_HTNbp, fgrf, N, BoxLen, kernel_transfer_function, &sp_HTNbp);
+            fft_apply_kernel(fbox_HTNbp, fgrf, N, BoxLen, kernel_transfer_function, &sp_HTNbp, x0, x1);
 
             /* Apply the inverse Poisson kernel -1/k^2 */
-            fft_apply_kernel(fbox_HTNbp, fbox_HTNbp, N, BoxLen, kernel_inv_poisson, NULL);
-
-            /* Fourier transform to real space */
-            fftw_plan c2r3 = fftw_plan_dft_c2r_3d(N, N, N, fbox_HTNbp, box_tshift, FFTW_ESTIMATE);
-            fft_execute(c2r3);
-            fft_normalize_c2r(box_tshift, N, BoxLen);
-            fftw_destroy_plan(c2r3);
+            fft_apply_kernel(fbox_HTNbp, fbox_HTNbp, N, BoxLen, kernel_inv_poisson, NULL, x0, x1);
+
+            /* Fourier transform to real space (node_rank 0 only) */
+            MPI_Win_fence(0, sh_win);
+            if (node_rank == 0) {
+                fftw_plan c2r3 = fftw_plan_dft_c2r_3d(N, N, N, fbox_HTNbp, box_tshift, FFTW_ESTIMATE);
+                fft_execute(c2r3);
+                fft_normalize_c2r(box_tshift, N, BoxLen, 0, N);
+                fftw_destroy_plan(c2r3);
+            }
+            MPI_Win_fence(0, sh_win);
 
             if (rank == 0 && pars->OutputFields) {
                 char vshift_fname[50];
                 sprintf(vshift_fname, "%s/gauge_vshift.hdf5", pars->OutputDirectory);
                 writeFieldFile(box_tshift, N, BoxLen, vshift_fname);
             }
-
-            /* Free the remaining complex grid */
-            free(fbox_HTNbp);
         }
 
         message(verbosity_low, "Applying N-body gauge transformation to the particles.\n");
@@ -1097,15 +1163,12 @@ long long run_fastdf(struct params *pars, struct units *us) {
             p->v[2] += vel[2] * inv_c * eps_eV * a_end;
         }
 
-        /* Free the gauge transformation boxes */
-        free(box_dshift);
-        free(box_tshift);
+        /* (box_dshift, box_tshift are shared and released with the window) */
     }
 
-    /* Free memory */
-    free(box);
-    free(fgrf);
-    free(fbox);
+    /* Release the shared-memory window and the node communicator */
+    MPI_Win_free(&sh_win);
+    MPI_Comm_free(&node_comm);
 
     /* Compute energies, masses, and weights and store them in contiguous arrays */
     double *energies = NULL;
